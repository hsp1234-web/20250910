# 第四階段計畫：成果分析與後續步驟

## 三、第三階段成果：以 Redis 為核心的非同步微服務工作流 (2025-09-19)

在前兩個階段成功驗證了微服務的獨立部署與測試能力後，第三階段的核心目標是解決服務間的**通訊問題**。我們成功地將一個核心業務流程，從傳統的 API 直接呼叫或背景任務模式，重構為一個以 Redis 訊息佇列為中心的、完全非同步的生產者/消費者模型。

### 1. 核心問題：緊耦合與同步阻塞

舊有的架構中，服務間的協作是緊耦合的。一個長時間的任務（如下載 -> 處理 -> 分析）會阻塞前端，或需要複雜的背景任務管理和狀態輪詢，這使得系統脆弱且難以擴展。

### 2. 解決方案：Redis 非同步工作流

我們引入了 Redis 作為系統的神經中樞，建立了一個標準化的非同步工作流。

#### a. 標準化設計
- **統一的訊息結構**：所有在 Redis 中傳遞的訊息都遵循一個標準的 JSON 結構，包含 `task_id`, `timestamp`, `source_service`, `status`, 和 `payload`，確保了通訊的規範性。
- **統一的任務追蹤**：每一個從開始到結束的完整流程，都由一個唯一的 `task_id` 進行追蹤。所有相關的服務（downloader, processor, analyzer）都透過這個 `task_id` 更新資料庫中的同一筆任務記錄，確保了資料的一致性。

#### b. 重構的服務與工作流程
我們成功地將核心工作流中的三個關鍵服務進行了徹底改造：

1.  **`downloader_service` (生產者)**:
    - **職責**：作為工作流的起點。
    - **改造**：當使用者透過 API 觸發下載後，`downloader` 不再僅僅是下載檔案。它會先在資料庫中建立一個帶有唯一 `task_id` 的任務記錄。下載成功後，它會組裝一個標準的 JSON 訊息，並將其發布到 Redis 的 `tasks:download_complete` 頻道。

2.  **`processor_service` (消費者 & 生產者)**:
    - **職責**：承上啟下，負責檔案的內容提取。
    - **改造**：此服務在背景啟動一個永久執行的監聽器，持續監聽 `tasks:download_complete` 頻道。一旦收到訊息，它會根據 `payload` 中的檔案路徑進行內容提取。完成後，它會將提取出的文字等資訊加入 `payload`，並發布一條新的訊息到 `tasks:processing_complete` 頻道。

3.  **`analyzer_service` (消費者)**:
    - **職責**：工作流的終點，負責 AI 分析。
    - **改造**：此服務同樣在背景監聽 `tasks:processing_complete` 頻道。收到訊息後，它會執行核心的 AI 分析邏輯，並將最終結果（如分析產生的 JSON 檔案路徑、使用的模型等）更新回資料庫中對應 `task_id` 的任務記錄，並將任務狀態標記為 `analysis_complete`。

#### c. 服務的獨立與解耦
- **遷移剩餘服務**：我們遵循先前 PoC 的模式，將 `backup_service` 和 `ingestion_service` 也重構為架構上獨立的服務，並將其前端路由改造為純粹的代理。這使得系統的模組化程度大大提高。
- **健壯的測試**：為所有新建立和改造的服務（包括 Redis 工作流本身）都編寫了自動化測試，確保了每個元件的可靠性和整體流程的正確性。

### 3. 成果與結論

本次重構取得了決定性的成功。我們不僅僅是拆分了幾個服務，而是建立了一套**可擴展、高彈性、高可靠性的非同步微服務架構**。

- **高效能**：前端請求可以立即得到回應，所有耗時操作都在後端非同步執行，極大地改善了使用者體驗。
- **高彈性**：每個服務都可以獨立擴展。如果未來 AI 分析成為瓶頸，我們可以輕易地啟動多個 `analyzer_service` 實例，它們都會從 Redis 佇列中獲取任務，共同分擔負載。
- **高可靠性**：即使某個服務（如 analyzer）暫時離線，訊息依然會保留在 Redis 佇列中。一旦服務恢復，它可以繼續處理積壓的任務，確保了資料的不遺失。

這個以 Redis 為核心的架構為專案的未來發展奠定了堅實的基礎，是向現代化、雲原生應用邁出的關鍵一步。
