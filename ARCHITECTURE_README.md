# 微服務架構與啟動流程優化說明

本文檔旨在說明本專案為改善冷啟動體驗而設計的微服務架構，特別是 `core_service` 的引入與作用。

## 1. 核心問題：冷啟動延遲

在舊的架構中，系統啟動時會循序載入所有服務，包括 UI 介面、資料處理功能以及重量級的 AI 分析服務。這導致使用者在訪問應用程式時，必須等待所有服務（包括最耗時的 AI 服務）都完全就緒後，才能看到第一個可用的頁面，使用者體驗不佳。

## 2. 解決方案：分層啟動與 `core_service`

為了解決這個問題，我們引入了「分層啟動」的策略，並建立了一個新的、輕量級的微服務：`core_service`。

### `core_service` 的職責

`core_service` 是一個**快速反應服務**，它的設計原則是**極度輕量、依賴極少**。它主要負責：

1.  **UI 渲染**：提供所有前端頁面的 HTML，讓使用者可以秒級看到應用程式的介面。
2.  **第一階段功能處理**：處理整個業務流程中最開始、且不依賴重量級套件的功能。在目前的實作中，這指的是「URL 提取」功能。

### 新的啟動流程

系統的協調器 (`src/core/orchestrator.py`) 現在採用兩階段啟動模式：

1.  **第一階段（同步、優先）**：
    *   協調器會**首先、且同步地**啟動 `core_service`。
    *   由於 `core_service` 的輕量特性，它幾乎可以瞬間就緒。
    *   一旦 `core_service` 啟動，系統的主要 URL 就已經可以訪問，使用者可以立即開始瀏覽頁面並使用第一階段的功能。

2.  **第二階段（非同步、背景）**：
    *   在 `core_service` 就緒後，協調器才會開始在背景啟動所有其他的重量級服務，例如主 API 伺服器（包含資料匯出等功能）和 `llm_service` 等。
    *   這些服務的啟動過程不會再阻塞使用者的初次訪問。

## 3. 優點

*   **極致的啟動體驗**：使用者不再需要等待漫長的後端初始化，可以立即與應用程式互動。
*   **更高的可用性**：即使後端的 AI 服務正在啟動或重啟，核心的 UI 和資料提交功能依然可用。
*   **清晰的職責分離**：架構更加清晰，`core_service` 專門處理使用者互動的前沿，而其他服務則專注於背景的重型計算任務。