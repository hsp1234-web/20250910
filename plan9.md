# plan9: 雙數據源策略與儀表板擴展指南 (最終版)

**版本:** 2.0 (權威版)
**日期:** 2025-09-28
**作者:** Jules

## 1. 核心結論：雙數據源策略 (Dual-Source Strategy)

經過對 `fredapi` 和 `OpenBB` 的多輪深度 POC，我們確立了一套清晰、高效的雙數據源策略，以指導您未來分析系統的開發：

1.  **`fredapi` -> 美國經濟基本盤 (The Baseline):**
    *   **定位:** 精密、可靠的**官方數據源**。
    *   **核心任務:** 獲取由聖路易聯準會 (FRED) 發布的美國權威總體經濟數據（CPI, GDP, 失業率, 利率等）。
    *   **結論:** 它是分析美國經濟基本面的**黃金標準**，穩定性與權威性無可替代。

2.  **`OpenBB` -> 全球視野與市場關聯 (The Extender):**
    *   **定位:** 強大的**多功能數據整合平台**與**價值放大器**。
    *   **核心任務:** 提供 `fredapi` 無法覆蓋的數據維度，實現分析能力的擴展。
    *   **結論:** OpenBB 的真正價值在於**「整合」**與**「擴展」**。我們已成功驗證，它能完美地做到：
        *   **獲取全球宏觀數據:** 透過其 `econdb` 等提供商，輕鬆獲取非美國經濟體的數據。
        *   **獲取市場行情數據:** 透過其 `yfinance` 等提供商，無縫接入股票、ETF 等市場數據。

**最終建議：建立一個以 `fredapi` 為數據基石，以 `OpenBB` 為能力擴展翼的強大分析系統。兩者並行，互相補足，能讓您的儀表板從單一的美國經濟監測器，升級為一個具備全球視野和宏觀-市場聯動分析能力的決策中心。**

---

## 2. 儀表板豐富化藍圖

基於雙數據源策略，我們能為您的儀表板設計以下具體的、可實現的擴展模組：

### 模組一：全球宏觀經濟對比 (由 OpenBB 驅動)
*   **功能:** 將美國、歐元區、中國等主要經濟體的關鍵指標（如 CPI, GDP）置於同一圖表進行橫向比較。
*   **價值:** 直觀地觀察全球經濟的聯動與分歧，例如通膨的全球傳導。
*   **實現數據源:** `OpenBB (econdb)`

### 模組二：宏觀-市場關聯分析 (雙數據源聯合驅動)
*   **功能:** 將來自 `fredapi` 的宏觀指標（如10年期美債利率 `DGS10`）與來自 `OpenBB` 的市場板塊ETF（如科技股 `XLK`）的價格走勢疊加在同一圖表上。
*   **價值:** 驗證宏觀經濟變化對市場的實際影響，將靜態的數據點轉化為動態的因果分析。
*   **實現數據源:** `fredapi` + `OpenBB (yfinance)`

### 模組三：市場熱點追蹤器 (由 OpenBB 驅動)
*   **功能:** 儀表板提供一個輸入框，輸入任一美股代碼（如 `TSLA`），即可看到其最新的股價走勢圖。
*   **價值:** 將宏觀分析與個股行情無縫結合，提供更全面的市場感知。
*   **實現數據源:** `OpenBB (yfinance)`

---

## 3. 開發實踐：最終 POC 腳本與範例

以下是我們最終產出的、可完美運行的 POC 腳本，它們是實現上述藍圖的技術基礎。

### 3.1. `poc/fred_data_fetcher.py` (獲取美國官方數據)

此腳本專注、高效地從 FRED 獲取美國核心總經數據。

*   **程式碼:** (已在 `poc/` 目錄下提供)
*   **執行前提:** 需在環境變數中設定有效的 `FRED_API_KEY`。
*   **預期成功輸出 (範例):**
    ```
    --- 開始從 FRED 獲取美國核心總經指標 ---

    ✅ 成功獲取: 消費者物價指數 (CPI) (CPIAUCSL)
      - 最新日期: 2025-08-01
      - 最新數值: 318.288

    ✅ 成功獲取: 實質國內生產毛額 (Real GDP) (GDPC1)
      - 最新日期: 2025-07-01
      - 最新數值: 22846.111
    ... (其他指標) ...
    ```

### 3.2. `poc/openbb_data_fetcher.py` (獲取全球視野與市場行情)

此腳本完美展示了 OpenBB 的擴展能力，且無需 API 金鑰即可運行。

*   **程式碼:** (已在 `poc/` 目錄下提供)
*   **成功運行輸出:**
    ```
    正在使用 OpenBB 獲取其獨有的市場行情數據...
    --- 開始從 OpenBB (yfinance) 獲取市場行情數據 ---

    ✅ 成功獲取: 蘋果公司 (AAPL) 股價
      - 最新數據預覽:
                      open        high         low       close    volume  dividend
    date
    2025-09-24  255.220001  255.740005  251.039993  252.309998  42303700       0.0
    2025-09-25  253.210007  257.170013  251.710007  256.869995  55202100       0.0
    2025-09-26  254.100006  257.600006  253.779999  255.460007  46045700       0.0
    --- 市場行情數據獲取完畢 ---
    ```

---

## 4. 附錄：外部資源參考

*   **FRED 官網:** [https://fred.stlouisfed.org](https://fred.stlouisfed.org) (查詢序列代碼)
*   **OpenBB 官方文件:** [https://docs.openbb.co](https://docs.openbb.co) (探索更多功能)
*   **Algotrading101 OpenBB 指南:** [https://algotrading101.com/learn/openbb-platform-guide/](https://algotrading101.com/learn/openbb-platform-guide/) (社群的最佳實踐)