# 金融數據解決方案研究報告

## 1. 研究目標

本研究旨在為我們的一級交易商壓力指數儀表板，尋找一個穩定、統一且易於維護的金融數據獲取方案。目前系統面臨數據源（特別是 FRED）API 金鑰失效、多個數據抓取器 (`fetcher`) 程式碼分散、難以維護的問題。我們需要一個現代化的解決方案來取代現有的數據層。

## 2. 候選方案評估

我們對市場上多個開源 Python 函式庫進行了深入研究，主要評估了 `qlab` 和 `OpenBB` 這兩個方案，並廣泛搜尋了其他潛在備選。

### 2.1. qlab (QF-Lib)

*   **定位分析**：`qlab`（其底層為 `QF-Lib`）是一個專為**量化金融分析**與**投資策略回測**設計的強大框架。它的核心功能是數據建模、風險管理與策略模擬。
*   **數據源能力**：它本身**不提供**數據，而是作為一個數據**消費者**，連接到外部數據源。其官方文件列出的主要數據提供商為 Bloomberg、Quandl、IB (Interactive Brokers) 等專業級付費終端。
*   **評估結論**：
    *   **不匹配**：它缺乏對我們核心數據源 **FRED** 的直接支援。若要使用，我們需要自行開發一個 FRED 的數據提供商，這與簡化系統的目標背道而馳。
    *   **過於複雜**：其功能（如事件驅動回測）對我們目前「獲取數據並繪圖」的需求來說，顯得過於龐大和複雜 (Over-engineered)。

**`qlab` 不符合我們的專案需求。**

### 2.2. OpenBB 平台

*   **定位分析**：OpenBB 是一個專門的**金融數據聚合平台**。它的核心目標就是提供一個統一的接口，讓開發者可以用一致的方式，存取來自數十個不同提供商的金融數據。
*   **數據源能力**：經過詳細核對，OpenBB **完美覆蓋**了我們所有的數據需求：
    *   **利率**：`openbb.fixedincome.sofr()` 直接提供 SOFR 數據。
    *   **公債**：`openbb.fixedincome.government.yield_curve()` 可獲取各期限公債殖利率，用於計算利差。
    *   **指數**：`openbb.index.price.historical(symbol='VIX')` 可獲取 VIX、MOVE 等波動率指數。
    *   **信用利差**：`openbb.fixedincome.corporate.ice_bofa()` 可獲取美銀指數，用於計算高收益債利差。
    *   **一級交易商數據**：`openbb.economy.primary_dealer_positioning()` 直接提供紐約聯儲 (NY Fed) 的一級交易商部位數據。
*   **評估結論**：
    *   **完美匹配**：它完美扮演了「數據抽象層」的角色，能讓我們用一套統一的程式碼取代所有分散的 `fetcher`。
    *   **簡化維護**：統一的 SDK 和標準化的憑證管理，將大幅降低未來新增數據或更換 API 金鑰的維護成本。
    *   **未來擴展性**：平台內建豐富的技術分析與計量經濟學工具，為專案未來的擴展提供了巨大潛力。

**`OpenBB` 是解決我們問題的理想方案。**

### 2.3. 其他備選方案

我們也廣泛搜尋了其他流行的金融函式庫，如 `TA-Lib`, `Zipline`, `QuantLib` 等。研究發現，這些函式庫要嘛是專注於**技術分析計算**，要嘛是專注於**演算法交易回測**。沒有任何一個像 OpenBB 一樣，專注於**聚合多個免費和付費的金融數據源**。

## 3. 綜合對比

| 特性 | qlab (QF-Lib) | OpenBB | 其他方案 (Zipline 等) |
| :--- | :--- | :--- | :--- |
| **核心定位** | 量化分析與回測框架 | **數據聚合平台** | 演算法交易與回測 |
| **FRED 數據支援** | 否 | **是** | 否（需自行整合） |
| **NY Fed 數據支援**| 否 | **是** | 否 |
| **統一數據接口** | 否（需自行開發 Provider） | **是** | 否 |
| **與專案契合度** | 低 | **高** | 低 |

## 4. 最終建議與下一步

**建議：**

我們應採用 **OpenBB** 作為我們新的數據服務層，全面重構現有的 `data_fetchers`。這將從根本上解決數據獲取失敗的問題，並大幅提升系統的健壯性、可維護性和擴展性。

**下一步實施計畫 (POC):**

1.  **環境安裝**：將 `openbb` 套件加入 `requirements.txt`。
2.  **單點重構**：選取 `fred_sofr_fetcher.py` 作為試點，將其程式碼改為使用 `openbb.fixedincome.sofr()` 獲取數據，並驗證其能與現有系統無縫對接。
3.  **憑證整合**：修改 `key_service`，將 FRED API 金鑰透過 OpenBB 的標準方式進行管理。
4.  **評估與推廣**：在 POC 成功後，制定將所有 `data_fetchers` 遷移至 OpenBB 的完整計畫。